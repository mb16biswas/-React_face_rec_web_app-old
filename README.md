# -React_face_rec_web_app-old-versionsion
it is a mern stack web app , the user  authentication is done by local storage   , a user can submit his image url  in the input box . the  web app can detect  the face using  Clarifai api  
##clone this repo

#Run npm install

#Run npm start

#you must have your clarifai api key and mongodb url
